# requirements.txt

# HTTP requests
requests==2.32.4

# Vector database client
qdrant-client==1.11.1

# Retry logic
tenacity==8.0.1

# Type hinting support for Python 3.7+
typing-extensions==4.12.2

# Environment variable management (optional, but recommended)
python-dotenv==0.19.2

# Testing framework (for writing and running tests)
pytest==7.0.1

# For creating more sophisticated CLIs (if needed)
click==8.0.3

# For working with CSV files (if needed beyond standard library csv module)
pandas==1.3.5

# OpenAI
openai==1.43.0

# Anthropic
anthropic==0.34.2

# Web interface actions
playwright
googlesearch-python
markdownify

# Mistral AI
mistralai==1.1.0

# Async operations
aiohttp==3.10.10

# For API
fastapi==0.115.4
uvicorn==0.32.0
anyio>=4.4.0 # not directly required, pinned by Snyk to avoid a vulnerability
h11>=0.16.0 # not directly required, pinned by Snyk to avoid a vulnerability
numpy>=1.22.2 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.5.0 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
